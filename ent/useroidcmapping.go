// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"glintfed/ent/user"
	"glintfed/ent/useroidcmapping"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// UserOidcMapping is the model entity for the UserOidcMapping schema.
type UserOidcMapping struct {
	config `json:"-"`
	// ID of the ent.
	ID uint64 `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID uint64 `json:"user_id,omitempty"`
	// OidcID holds the value of the "oidc_id" field.
	OidcID string `json:"oidc_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt *time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt *time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserOidcMappingQuery when eager-loading is set.
	Edges        UserOidcMappingEdges `json:"edges"`
	selectValues sql.SelectValues
}

// UserOidcMappingEdges holds the relations/edges for other nodes in the graph.
type UserOidcMappingEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserOidcMappingEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserOidcMapping) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case useroidcmapping.FieldID, useroidcmapping.FieldUserID:
			values[i] = new(sql.NullInt64)
		case useroidcmapping.FieldOidcID:
			values[i] = new(sql.NullString)
		case useroidcmapping.FieldCreatedAt, useroidcmapping.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserOidcMapping fields.
func (_m *UserOidcMapping) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case useroidcmapping.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = uint64(value.Int64)
		case useroidcmapping.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				_m.UserID = uint64(value.Int64)
			}
		case useroidcmapping.FieldOidcID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field oidc_id", values[i])
			} else if value.Valid {
				_m.OidcID = value.String
			}
		case useroidcmapping.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = new(time.Time)
				*_m.CreatedAt = value.Time
			}
		case useroidcmapping.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = new(time.Time)
				*_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserOidcMapping.
// This includes values selected through modifiers, order, etc.
func (_m *UserOidcMapping) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserOidcMapping entity.
func (_m *UserOidcMapping) QueryUser() *UserQuery {
	return NewUserOidcMappingClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this UserOidcMapping.
// Note that you need to call UserOidcMapping.Unwrap() before calling this method if this UserOidcMapping
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *UserOidcMapping) Update() *UserOidcMappingUpdateOne {
	return NewUserOidcMappingClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the UserOidcMapping entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *UserOidcMapping) Unwrap() *UserOidcMapping {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserOidcMapping is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *UserOidcMapping) String() string {
	var builder strings.Builder
	builder.WriteString("UserOidcMapping(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserID))
	builder.WriteString(", ")
	builder.WriteString("oidc_id=")
	builder.WriteString(_m.OidcID)
	builder.WriteString(", ")
	if v := _m.CreatedAt; v != nil {
		builder.WriteString("created_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	if v := _m.UpdatedAt; v != nil {
		builder.WriteString("updated_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// UserOidcMappings is a parsable slice of UserOidcMapping.
type UserOidcMappings []*UserOidcMapping
